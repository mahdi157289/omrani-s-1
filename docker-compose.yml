version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      # Add other environment variables here if needed

  client:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - server
